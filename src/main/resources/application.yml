server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/article
    username: root
    password: wls960aha!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379